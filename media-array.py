nota1=float(input("Qual a nota da 1ª prova (caso deja parar, digite SAIR): "))
nota2=float(input("Qual a nota da 2ª prova (caso deja parar, digite SAIR): "))

def calcMedia(nota1, nota2):
    media=(nota1 + nota2)/2
    print(media)

calcMedia()
